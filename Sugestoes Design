homePage

mobile - carrossel pequeno ------ CHECK



desktop - icones navbar muito espaçados(muito espaço vazio), trocar ordem (search no meio, perfil a direita)  ------ CHECK
	tabs new e best seller parecem botoes ---- CHECK

	faq e ticket us demasiado realçados, talvez mudar para link ------ CHECK



adminProfilePage ------ CHECK

	dentro de cada colapsável, o botão de colapsar e de adicionar novo  ------ CHECK
	têm o mesmo peso, devemos remover o peso do botão (sugestao, tirar ------ CHECK
	o fundo azul do botao, e tornar o triângulo azul) ------ CHECK

adminProfile/userProfile/register ------ CHECK

	alinhar email com idade, ou colocar os 3 campos numa coluna  ------ CHECK
	unica. alterar o campo de upload (por exemplo, um "+" ou simbolo de lapis ------ CHECK
	na imagem do admin. dar menos peso ao botao delete, geralmente tenta-se ------ CHECK
	"esconder" este tipo de opções (sign-out, apagar conta, apagar comentario, ------ CHECK
	etc) ------ CHECK

	acrescentar asterisco nos campos facultativos (convem haver   ------ CHECK
	distincao, e desta forma em geral obtemos mais dados)   ------ CHECK

	possivelmente ter paginas distintas de login e register  ------ CHECK
	
carrinho ------ CHECK

	retirar importancia ao continue shopping, dar mais importancia ao   ------ CHECK
	proceed with purchase (por exemplo, no continue shopping retirar  ------ CHECK
	o fundo azul, deixar so uma border)  ------ CHECK
	muito pouco espaco utilizado, aproveitar espaço na horizontal,  ------ CHECK
	sugestao: semelhante ao da worten ------ CHECK
	
phonePage

	alterar o rating, nao e intuitivo o que significa. manter um tema 
	para os botoes, botao add to cart diferente de todos os outros

purchaseHistory

	pequenos jumbotrons para assinalar cada estado de envio. em baixo de 
	cada um deles os produtos (em linha)

	OU

	organizar por compra (se compramos 3 telemoveis de uma vez, mostra na 
	pagina a compra aglomerada)


wishlist

	botao de comprar agora pouco intuitivo (por exemplo, acrescentar um label)
	pesquisar na wishlist, ou pagina-las
	tirar carrossel

searchScreen ----- CHECK
	
	adicionar header e navbar. pelo menos em deskop, mostrar de imediato os ----- CHECK
	filtros, em vez de ser sidebar (ha muito espaço livre no ecra)   ----- CHECK


admin gestao
	sidebar com as opcoes principais ( admin account, brands, faq, etc) e carregando
	ai aparece as opcoes individuais
	adicionar opcao de editar em vez de so remover


gitlab
	branch com a versao do codigo aquando da entrega do a3

	




